# ChemAI v33 优化内容完整报告

> 生成日期：2026-08-08  
> 版本：v33  
> 基础版本：v32.5（e425fda）

---

## 一、LaTeX 渲染全面修复

### 问题诊断
- **根本原因**：JSON 数据文件（`manual.json`、`faq_unified.json`、`kb.json`）中存在大量双重转义的 LaTeX 命令
  - `\\\\cdot` 在 JSON 中被解析为 JS 字符串 `\\cdot`（两个反斜杠）而非 `\cdot`（单反斜杠），导致渲染正则表达式无法匹配命令
  - 部分数据文件包含 CR（`\r`）字符，影响 JSON 解析稳定性
  - `faq_unified.json` 中的 `detail` 字段存在多处截断的 LaTeX 公式（来源引用提前终止）

### 修复内容
| 文件 | 修复项 | 数量 |
|------|--------|:--:|
| `data/manual.json` | 双重转义命令修复 | 28 |
| `data/manual.json` | 移除多余 CR 字符 | 2,786 |
| `data/faq_unified.json` | 双重转义命令修复 | 222 |
| `data/kb.json` | 双重转义命令修复 | 16 |
| `data/kg.json` | 移除多余 CR 字符 | 2,243 |

### 渲染器增强
**`main.html` — `renderFormula()` 函数：**
- 新增 `\Phi` → Φ、`\equiv` → ≡、`\Longrightarrow` → ⟹ 的 Unicode 映射
- 新增 `\hat{x}` → x̂ 处理（正则替换 `\hat{...}` → 组合字符）
- 扩展 `BARE` 恢复表：添加 `equiv`、`Phi`、`nu`
- 保留控制字符恢复逻辑（TAB、BS、VT、CR）

**`assistant.html` — `renderLatexBody()` 函数：**
- 新增双重反斜杠归一化 `s.replace(/\\\\/g,'\\')`，捕获 JSON 加载数据中的任何残留双重转义
- 新增 `\sqrt[n]{x}` → √(x)、`\xrightarrow[opt]{above}` → →(above) 处理
- 新增 `\bar{x}` → x̄、`\hat{x}` → x̂ 处理
- 新增残留花括号清理 `s.replace(/[{}]/g,'')`，处理来自 `\text{}`、`\mathrm{}` 等无内容命令的残留括号

---

## 二、知识图谱可视化增强

### 知识图谱数据 (`knowledge.html`)
**节点规模**：82 节点 / 115 关联（含 34 条带标签边）

| 功能 | 描述 |
|------|------|
| **边标签渲染** | 34 条带 `label` 属性的边现在以半透明白色文本 + 深色背景胶囊 (`rgba(10,14,26,.78)`) 形式渲染在边的中点位置，在聚焦/链路模式下与边的 alpha 同步淡入淡出 |
| **节点脉冲动画** | `selectNode()` 被调用时，被选中的节点触发三圈扩展的白色脉冲环（相位偏移 $0.35\pi$，在 1.5 秒内衰减），提供即时的视觉反馈 |
| **KaTeX 公式渲染** | 详情面板中的公式（50+ 条）现在通过 KaTeX JS v0.16.14（CDN 加载，SHA-384 完整性验证）渲染为正确的数学排版；在 KaTeX 不可用或抛出错误时，回退至原始的 HTML 下标/上标替换方案 |
| **迷你地图** | 舞台左下角新增 150×100px 的概览画布，显示所有可见节点的彩色圆点 + 表示当前视口的虚线矩形框（翡翠色描边），每帧随主画布一同渲染 |

### 交互改进
- `selectNode()` 现在设置 `n.flashUntil = performance.now() + 1500`，在 draw() 中触发脉冲动画
- 迷你地图使用 `min(devicePixelRatio, 2)` 以获得清晰渲染，同时控制内存开销
- 边标签字体：`9px "PingFang SC","Microsoft YaHei",sans-serif`，与世界空间字体缩放规则分离

---

## 三、实验报告评估可视化增强

### 报告页面 (`assistant.html`)

| 功能 | 描述 |
|------|------|
| **增强的雷达图** | 三轴六边形雷达图（理论 / 实验 / 综合），轴大小提升至 200×170px；4 个带有百分比标签（25%/50%/75%/100%）的虚线网格环；使用 `feGaussianBlur` 发光效果的数据多边形；以动画圆点呈现的数据顶点（`r` 从 0→5→4 过渡）；整体 `@keyframes radarIn` 缩放动画 |
| **知识点评分条形图** | `dimensionBreakdown()` 函数生成一个内联 SVG 水平条形图，展示全部 12 个知识点按分数从高到低排序；颜色编码：绿色 ≥ 85%、青色 ≥ 60%、黄色 ≥ 30%、红色 < 30%；带有交错延迟的动画条形 |
| **详细综合评语** | `overallSummary()` 函数（约 80 行）根据五个等级（优秀 / 良好 / 中等 / 及格 / 不及格）生成叙述性评估文本，包含特定知识点评语、三维度诊断（每个维度有自定义描述性文本）、以及可操作的学习建议 |
| **打印样式表** | 完整的 `@media print` 规则：隐藏导航栏、侧边栏、按钮、输入框；将深色主题替换为干净的黑色文本 + 白色背景；报告卡片以全 A4 宽度呈现 |
| **计时统计** | 四个计时摘要卡片：总用时（分钟）、总用时（秒）、平均每题用时（秒）和对话轮次，使用 flexbox 响应式布局 |

### 导出增强
- **Word (.doc)**：现在包含详细的综合评语（通过剥离 HTML 标签适配 Word 渲染）
- **TXT (.txt)**：现在包含去除了 HTML 标签的纯文本综合评语段落

---

## 四、样式调整

### 公式非斜体化
- `main.html` 中的 `.fx` 类从 `font-style:italic` 改为 `font-style:normal`，化学式以直立字体显示，提升可读性

### 内容精简
- 从 `manual.json` 中移除了「第 11 章：实验报告撰写规范」（5 个小节：报告结构、数据记录、误差分析、作图规范、文献引用），剩余章节重新编号为 1-11 章

---

## 五、AI 助手智能体集群

### 四阶段流水线架构

```
用户提问
  ↓
阶段 1：多策略检索
  ├─ 检索 1：直接关键词搜索语料库（BM25 加权）
  ├─ 检索 2：化学实体类比检索（同子领域相似结构/性质条目）
  └─ 检索 3：方法学转移检索（相同操作术语跨体系匹配）
  ↓
阶段 2：类比推理引擎
  ├─ 比较问题术语与 ANALOG_TABLE（25 组概念映射）
  ├─ 识别跨化学体系的模式（K₃[Fe(C₂O₄)₃] → K₃[Cr(C₂O₄)₃] 等）
  └─ detectChemBridge() 处理 5 种超出领域的问题类型
  ↓
阶段 3：置信度评分 + 混合答案生成
  ├─ confidenceScore() 计算（语料库 0-1 + FAQ 0 或 0.9 + 类比 0-1）
  ├─ 高置信度 + 直接命中 → 标准模板答案
  ├─ 中等/无直接匹配 → 类比桥接 + 通用化学知识模板
  └─ 低置信度 → LLM 回答（如已配置）或带明确边界的网络回退
  ↓
阶段 4：自我检查
  └─ 6 项验证：Fe²⁺/Fe³⁺ 混淆、配位数错误、电荷守恒违规、
      电解质类型不正确、安全性表述不准确、温度参数合理性
```

### 新增函数（9 个）

| 函数 | 行号 | 描述 |
|------|:--:|------|
| `retrieval2_chemicalAnalogy(q)` | 575 | 通过 ANALOG_TABLE 扫描问题，搜索具有相似概念的语料库条目，通过金属离子进行家族类比 |
| `retrieval3_methodologyTransfer(q)` | 615 | 检测操作术语，找到所有共享相同方法的语料库条目 |
| `analogicalReasoning(q, ...)` | 639 | 在 ANALOG_TABLE 内部 / 外部比较问题与已知概念，调用 `detectChemBridge` 进行桥接 |
| `detectChemBridge(q)` | 687 | 为 5 种超出领域的问题提供特定桥接解释（电导率、稳定性比较、制备类比、表征、颜色） |
| `confidenceScore(...)` | 726 | 通过 0-1 分数加权计算多来源置信度，返回带有级别标签（高/中/低）的结果 |
| `buildHybridAnswerHTML(q, ctx)` | 751 | 根据置信度层级路由答案：高 → 标准，中 → 类比桥接，低 → 带边界的通用模板 |
| `buildAnalogyBridgedAnswer(q, ctx)` | 768 | 构建 5 部分答案：核心 + 类比面板 + 类比来源 + 直接文献 + 网络回退 |
| `selfCheck(q, answerHTML, ctx)` | 833 | 对照配位化学基础知识验证答案自洽性 |
| `selfCheckHTML(result)` | 877 | 将自检结果格式化为可视的警告 / 通过标记 |

### ANALOG_TABLE 知识图谱
**25 组概念映射**涵盖：

| 领域 | 条目 |
|------|------|
| 配位化学 | 草酸根配位、三草酸合铁(III)酸钾、三草酸合铬(III)酸钾、三草酸合铝(III)酸钾、三草酸合钴(III)酸钾 |
| 合成方法 | 莫尔盐路线、H₂O₂ 氧化、乙醇析晶、沉淀法、重结晶 |
| 表征技术 | 红外光谱、磁化率测定、TG-DSC、UV-Vis 吸收光谱、循环伏安 |
| 理论 | 晶体场理论、配体场理论、价键理论、螯合效应 |
| 性质 | 导电性、光化学活性、pH 影响、产率优化、安全操作 |

### 流水线 UI 变更
- 流水线现在显示 **4 个步骤**（从 3 个增加），并带有新的"类比推理引擎"步骤
- 新增置信度条形图（绿色/黄色/红色），包含数值百分比和级别标签
- 新增 `.source-tag` 标签，以颜色编码信息来源（蓝色=语料库、紫色=类比、黄色=通用、绿色=FAQ）
- 如果自检发现违规，新增黄色警告横幅

---

## 六、移动端响应式增强

### 全局改进
| 页面 | 改进内容 |
|------|------|
| **全部页面** | 导航链接在 ≤640px 时添加汉堡菜单切换；按钮最小触摸目标调整为 44×44px；间距在 ≤640px 时减小；字号在移动端提升至最小 14px 以兼容 iOS 缩放 |
| **main.html** | 在 ≤640px 时：手册网格从 2 列转为 1 列；卡片内边距减半；标题字号缩小；目录隐藏（移动端下拉选择器保持可见） |
| **assistant.html** | 在 ≤640px 时：侧边栏堆叠在聊天面板下方；网格转为单列布局；聊天气泡使用更宽的最大宽度（94%）；评估进度条宽度适配 |
| **knowledge.html** | 在 ≤640px 时：图例面板变小（`max-width:180px`）；舞台提示文本隐藏；面板滑动至全屏覆盖（`left:8px;right:8px`） |
| **corpus.html** | 在 ≤640px 时：表格获得 `overflow-x:auto` 包装器；搜索/筛选字段堆叠为垂直布局；分页控件包裹并居中 |

### 断点定义
```css
@media (max-width:1080px) { /* 平板横屏：侧边栏堆叠 */ }
@media (max-width:900px)  { /* 平板竖屏：减小元素尺寸 */ }
@media (max-width:640px)  { /* 手机横屏：全面单列布局 */ }
@media (max-width:360px)  { /* 小手机：极限优化 */ }
```

---

## 七、代码优化

### 冗余代码清理
| 页面 | 改动 |
|------|------|
| **assistant.html** | 移除 `faqReady` 的重复检查（`loadFAQ` 函数中的冗余标志）；将重复的 `delay()` 实用函数合并为一个共享实例；移除 LLM 密钥处理中的死代码分支 |
| **main.html** | 移除 `renderFormula()` 中的重复 G 映射键（`rightarrow` 重复）；将硬编码的基准 URL 变量化；移除未使用的 `formulas` 数组处理 |
| **knowledge.html** | 将重复的节点查找模式合并为实用函数；移除 `draw()` 中迭代可见 / 不可见节点时的冗余循环；清理未使用的 KaTeX 渲染路径 |
| **corpus.html** | 移除未使用的 `refreshSubfieldOptions` 调用；合并重复的 `createElement` 模式 |

### 数据层
- `manual.json` 中的所有 5 个 JSON 文件均验证为格式良好的 JSON
- 移除了重复的跨章节内容引用
- 在所有条目中规范化了 `subfield` 值，以与 `categories.json` 保持一致

---

## 八、README 重写

完全重写的 README.md：
- 更新版本号至 **v33**
- 新增 **智能体集群流水线架构**部分，包含 4 阶段图表
- 更新知识图谱统计数据（82 节点 / 115 关联）
- 新增 **类比知识库**部分
- 新增 **掌握度测评**部分，包含报告功能
- 精简的 **版本历史**，展示完整的 v31→v33 演进
- 更新项目结构树，反映当前文件状态
- 移除了部分工具脚本的详细文档（保留引用）

---

## 九、文件变更统计

| 文件 | 变更类型 | 大致行数变更 |
|------|------|:--:|
| `data/manual.json` | 修复 + 删除章节 | -2786 / +2588 |
| `data/faq_unified.json` | 修复 | ~222 条修复 |
| `data/kb.json` | 修复 | ~16 条修复 |
| `data/kg.json` | 修复 | ~2243 个字符 |
| `main.html` | 修复 + 增强 | ~+50 行 |
| `assistant.html` | 重大增强 | ~+500 行 |
| `knowledge.html` | 重大增强 | ~+244 行 |
| `corpus.html` | 增强 | ~+30 行 |
| `prep.html` | 增强 | ~+27 行 |
| `README.md` | 重写 | -150 / +90 行（净减少，更简洁） |

---

## 十、部署

- **仓库**：`github.com/LittleAlety/chemai-8.6-`
- **部署方式**：GitHub Actions → GitHub Pages（master 分支自动部署）
- **在线地址**：`https://littlealety.github.io/chemai-8.6-/`
- **提交总数**：9 次提交（155a881 → f479c8e）

---

## 十二、移动端响应式增强（补充）

经过三个并行 agent 集群的协同工作，所有 7 个页面（6 个 HTML + 1 个 CSS）均已通过移动端响应式审计并修复：

| 页面 | 主要移动端改进 |
|------|------|
| `main.html` | 汉堡菜单 + 导航折叠；360px 极限断点；44px 最小触摸目标 |
| `assistant.html` | 侧边栏在 ≤640px 时垂直堆叠；Tab 标签页垂直排列；900px 平板断点 |
| `knowledge.html` | 汉堡菜单；图例面板在 ≤360px 时隐藏；工具栏间距优化 |
| `prep.html` | 汉堡菜单；卡片内边距缩减；44px 按钮最小高度 |
| `corpus.html` | 汉堡菜单；工具栏垂直堆叠；表格最小宽度缩减至 400px |
| `index.html` | 在编译后的 CSS 末尾新增移动端覆盖规则（Flexbox 网格 → 单列） |

**通用移动端规范：**
- 3 级响应式断点：1080px（平板横屏）/ 640px（手机）/ 360px（小手机）
- 所有交互元素最小触摸目标：44×44px
- 移动端最小正文字号：12-14px（修复 iOS 自动缩放问题）
- 水平内边距在 640px 断点从 24px 缩减为 10-14px
- 所有 `<table>` 元素包裹在 `overflow-x:auto` 容器中

## 十三、FAQ 倒排索引缓存（性能优化）

### 三层渐进式缓存架构
```
第 1 层：sessionStorage（同浏览器会话复用，0ms 延迟）
  ↓ 未命中
第 2 层：fetch + HTTP 304 Not Modified（force-cache，零传输）
  ↓ 失败
第 3 层：内置 FAQ_FALLBACK（15 条核心 FAQ 始终可用，不依赖网络）
```

### 倒排索引结构
- `FAQ_IDX.keys = {normKey: [faqIdx, ...]}` — 关键词 → FAQ 条目位置
- `FAQ_IDX.ents = {normEnt: [faqIdx, ...]}` — 化学实体 → FAQ 条目位置
- 构建成本：O(N) 一次遍历（约 700 条 → < 5ms）
- 查询收益：`matchFAQ` 从 O(N) 全量扫描 → O(候选集) 索引查找，典型候选集 < 10 条

### 代码变更
| 函数 | 说明 |
|------|------|
| `buildFAQIndex(src)` | 为 keys/ents/标题词构建倒排索引 |
| `loadFAQ()` | 三层层级：sessionStorage → fetch → 兜底，加载后自动写入 sessionStorage |
| `matchFAQ(q)` | 提取问题的二元组/三元组词 → 倒排索引查找候选 → 精细打分 |
| `relatedFAQs(q,exclude,n)` | 同样使用索引加速的 FAQ 推荐 |

---

## 十四、知识清单问题覆盖宽度优化

诊断发现 FAQ 关键字和语料库问题存在严重覆盖不足：
- FAQ 条目平均仅 5.9 个关键字，420/700 条 ≤5 个
- 语料库 232/272 个独特问题是模板化的（"X 的主要研究内容是什么？"）
- 自然提问形式（"怎么做""为什么""是什么"）严重缺失

编写了两个 Node.js 扩充脚本（`scripts/enrich-faq-keys.js`、`scripts/enrich-corpus-questions.js`），应用五条规则：

**FAQ 扩充结果：**
| 指标 | 之前 | 之后 | 增幅 |
|------|------|------|:--:|
| 总关键字数 | 4,121 | 17,086 | +315% |
| 平均关键字数/条 | 5.9 | 24.4 | +313% |
| 总实体数 | 1,445 | 10,721 | +642% |
| ≤5 条关键字的条目 | 420 | 0 | -100% |
| "怎么做"覆盖 | 8 条 | 602 条 | - |
| "是什么"覆盖 | 7 条 | 1,976 条 | - |

**语料库扩充结果：**
| 指标 | 之前 | 之后 | 增幅 |
|------|------|------|:--:|
| 总问题数 | 598 | 2,674 | +347% |
| 平均问题数/条 | 2.1 | 9.2 | +338% |
| 模板化问题 | 232 | 0 | -100% |
| 独特问题模板 | 272 | 637 | +134% |

**扩充规则：**
- 规则 A：自然提问变体（怎么做/为什么/是多少/有哪些/叫什么）
- 规则 B：化学同义词扩展（制备=合成、配位=络合、废液=废物等）
- 规则 C：实体充实（中英文 + 化学式双名：三草酸合铁酸钾、K₃[Fe(C₂O₄)₃]·3H₂O、Potassium ferrioxalate）
- 规则 D：跨条目交叉引用（关联概念关键词互相链接）
- 规则 E：去重与修剪（每条目最多 25 个关键词，基于评分优先级排序）

*报告由 ChemAI 团队自动生成 — 2026-08-08*
